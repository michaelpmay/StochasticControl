%makeHisteresisFigure
addpath classes\
build=ModelFactory;
model=build.autoregulatedModelWithUniformInput(0);
modelOde=SolverODE(model);
analyzer=HisteresisAnalysis(modelOde);
[upAnalysis,downAnalysis]=analyzer.analyze;
